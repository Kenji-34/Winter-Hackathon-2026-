# Supabase — safe to expose in client bundle (Vite inlines VITE_* vars)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# OpenAI — SERVER ONLY. Never prefix with VITE_; never import inside src/
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
